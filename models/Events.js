class Events {
  constructor(id, name, place, paid, images, description) {
    this.id = id;
    this.name = name;
    this.place = place;
    this.paid = paid;
    this.images = images;
    this.description = description;
  }
}

export default Events;
